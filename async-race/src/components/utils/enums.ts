export enum BtnClass {
  REMOVE = 'btn remove-btn',
  SELECT = 'btn select-btn',
  START_ENGINE = 'start-engine-btn',
  STOP_ENGINE = 'stop-engine-btn',
}

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
